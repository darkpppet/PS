using System;
using System.Numerics;

BigInteger sum = BigInteger.Parse(Console.ReadLine());
BigInteger diff = BigInteger.Parse(Console.ReadLine());

Console.WriteLine((sum + diff) / 2);
Console.WriteLine((sum - diff) / 2);
